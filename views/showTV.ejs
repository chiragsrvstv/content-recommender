<% include partials/header %>

<div class=" ui container">
  <img class="ui medium image" src="https://image.tmdb.org/t/p/original<%=tvContent.poster_path;%>" alt="poster image">
  <p> <%= tvContent.name %> </p>
  <h2> Overview </h2>
  <p> <%= tvContent.overview %> </p>
  <h3><%= tvContent.vote_average %> </h3>



<div>
  <!-- form's action is bit messed up, coz checking here if username is null -->
  <form class="ui form" action="/tonight/approved/access/
  <% if(user){ %>
    <%= 'random' %>
  <%} else { %>
    <%= user %>
  <%}%>
              /tv/show/<%=tvContent["id"]%>" method="POST">
  <input type="range" name="ratings" min="0" max="10">
  <button type="submit" class="ui button"> Rate it </button>
</form>
</div>

</div>
<% include partials/footer %>
